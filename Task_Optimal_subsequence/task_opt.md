# Задача Оптимальная подпоследовательность

- **Имя входного файла:** стандартный ввод
- **Имя выходного файла:** стандартный вывод
- **Ограничение по времени:** 1 с
- **Ограничение по памяти:** 256 МБ

Дан массив $a$ из $n$ положительных целых чисел и число $m$. Выберите подпоследовательность $a_{i_1}, a_{i_2}, \dots, a_{i_k}$ ($0 \le k \le n$), чтобы значение $\left(\sum_{j=1}^k a_{i_j}\right) \bmod m$ было максимальным. Подпоследовательность может быть пустой.

## Формат входных данных

В первой строке заданы два числа $n$ и $m$ ($1 \le n \le 40$, $1 \le m \le 10^9$).

Во второй строке записаны $n$ целых чисел $a_1, a_2, \ldots, a_n$ ($1 \le a_i \le 10^9$).

## Формат выходных данных

Выведите максимальное значение $\left(\sum_{j=1}^k a_{i_j}\right) \bmod m$.

## Полезный комментарий

Компилятор: Microsoft Visual C++ 15.9 (2017) x64.

Чтобы ускорить ввод-вывод, добавьте:

```cpp
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
}
```

> solution.cpp(1): fatal error C1083: Cannot open include file: 'bits/stdc++.h': No such file or directory

## Пример 1

**Ввод**

```text
4 4
5 2 4 1
```

**Вывод**

```text
3
```

## Пример 2

**Ввод**

```text
3 20
199 41 299
```

**Вывод**

```text
19
```
